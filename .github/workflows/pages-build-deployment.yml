
    name: Force Pages Build
    
    on:
      workflow_dispatch:
    
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v3
          - name: Empty Commit
            run: |
              git commit --allow-empty -m "Trigger pages build"
              git push
    
